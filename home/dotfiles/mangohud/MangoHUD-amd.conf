# MangoHUD config — overlay de rendimiento
# Sin límite de FPS: la PC AMD no lo necesita

# Overlay
position=top-left
font_size=24
background_alpha=0.4
background_color=1a1a2e

# Métricas
gpu_stats
gpu_temp
gpu_power
cpu_stats
cpu_temp
ram
vram
engine_version
frametime=0
frame_timing

# Colores
gpu_color=2e86de
cpu_color=a55eea
ram_color=ff6b6b
vram_color=feca57
engine_version_color=1dd1a1
